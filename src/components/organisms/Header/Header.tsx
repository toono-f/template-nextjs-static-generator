import styles from "@/components/organisms/Header/Header.module.scss";

export const Header = () => {
  return (
    <header className={styles.header}>
      <p>headerです</p>
    </header>
  );
};
