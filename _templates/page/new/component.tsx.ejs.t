---
to: src/components/templates/<%= path.charAt(0).toUpperCase() + path.slice(1); %>/index.tsx
---
export const <%= path.charAt(0).toUpperCase() + path.slice(1); %> = () => {
  return (
    <>
      <div></div>
    </>
  );
};
